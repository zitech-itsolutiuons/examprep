# --- Database ---------------------------------------------------------------
DATABASE_URL="postgresql://postgres:Ticx%28one%2912@localhost:5432/examprep?schema=public"

# --- NextAuth ----------------------------------------------------------------
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="replace-with-a-long-random-string" # `openssl rand -base64 32`

# --- Email (password reset) --------------------------------------------------
EMAIL_SERVER_HOST="smtp.example.com"
EMAIL_SERVER_PORT="587"
EMAIL_SERVER_USER="apikey"
EMAIL_SERVER_PASSWORD="replace-me"
EMAIL_FROM="ExamPrep <no-reply@examprep.app>"

# --- App -----------------------------------------------------------------
APP_URL="http://localhost:3000"
